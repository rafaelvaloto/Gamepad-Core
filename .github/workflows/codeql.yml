name: "CodeQL"

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  schedule:
    - cron: '30 18 * * 6'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'cpp' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        config: |
          paths-ignore:
            - 'Source/Public/Libs/miniaudio.h'
            - 'Source/Private/GImplementations/Utils/MiniAudio/miniaudio_impl.cpp'
            - 'Source/Public/Libs/**'
            - '**/*.wav'
            - 'Tests/Integration/Datasets/**'
        queries: security-extended,security-and-quality

    - name: Install Dependencies (Linux)
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake libasound2-dev libudev-dev libsdl2-dev

    - name: Configure CMake
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTS=ON

    - name: Build
      run: cmake --build ${{github.workspace}}/build --config Release

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"
